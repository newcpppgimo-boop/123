const q=new URLSearchParams(location.search);if(q.get('type')==='concern')document.getElementById('voiceType').value='concern';
document.getElementById('voiceForm')?.addEventListener('submit',async e=>{e.preventDefault();const x=await requireYouth();if(!x)return;voiceMsg.textContent='Submitting…';const {error}=await supabaseClient.from('youth_voice').insert({user_id:x.user.id,type:voiceType.value,category:voiceCategory.value,message:voiceMessage.value.trim(),anonymous:voiceAnon.checked,status:'pending'});voiceMsg.textContent=error?error.message:'Thank you. Your submission has been recorded.';if(!error)e.target.reset()});